name: Build and Publish Teraslice Asset
run-name: ${{ github.actor }} is building and publishing the Teraslice Asset
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  call-asset-build:
    uses: terascope/workflows/.github/workflows/asset-build-and-publish.yml@f52b2a36f69ae044270d12fff5ccf4a269a48cc5
    secrets: inherit
