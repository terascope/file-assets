name: Build and Publish Teraslice Asset
run-name: ${{ github.actor }} is building and publishing the Teraslice Asset
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  call-asset-build:
    uses: terascope/workflows/.github/workflows/asset-build-and-publish.yml@e304cf5f8448020289db040f0fe14402dd2b2327
    secrets: inherit
